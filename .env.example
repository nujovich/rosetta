# Conexión a Postgres / Supabase para la etapa de carga (load).
# Copiá a .env y completá. NUNCA commitees .env.
DATABASE_URL=postgres://user:pass@host:5432/femicidios
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
